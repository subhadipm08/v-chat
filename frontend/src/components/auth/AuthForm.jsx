/**
 * AuthForm is no longer used — the multi-step auth flow is now
 * fully handled inside src/pages/Auth.jsx.
 * This file is kept as a placeholder to avoid any stale import errors.
 */
export default function AuthForm() {
  return null;
}
